import { defineConfig } from 'astro/config';

// https://astro.build
export default defineConfig({
  site: 'https://ytilikan.github.io',
  // base: '/ytilikan-site', // décommenter si déployé sous un sous-chemin GitHub Pages
});
